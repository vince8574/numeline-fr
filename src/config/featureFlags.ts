// Masquage RÉVERSIBLE de fonctionnalités : passer le flag à `true` pour réactiver,
// sans rien re-coder (le code reste en place, juste non affiché).

// « Mode malvoyant » / guidage vocal (toggle Accueil + Paramètres). Masqué pour
// l'instant ; repasser à true pour le réafficher intégralement.
// Typé `boolean` (et non littéral `false`) pour éviter les avertissements
// "condition toujours fausse" et garder les variables/imports considérés utilisés.
export const SHOW_ACCESSIBILITY: boolean = false;
